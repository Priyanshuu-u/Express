<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
</head>
<body>
    <form action="http://localhost:8080/ig" method="get">
        <input type="text"><button>search</button>

    </form>
    <h1>Account of @<%= data.name%></h1>
    <h3>Followers: <%= data.followers %> Following: <%=data.following%></h3>
    <button>Follow</button><button>message</button>
    <hr>
    <% for(let post of data.posts){%>
    <img src="<%=post.image %>" alt="user Image">
    <p>
        Likes: <%= post.likes %> Comments: <%= post.comments %>
    </p>
    <% } %>
    
</body>
</html> -->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <style>
        body {
            text-align: center;
        }
        img{
            width: 100px;
            height: 100px;
        }
    </style>
</head>

<body>
    <form action="/ig/search" method="get">
        <input type="text" name="username">
        <button type="submit">Search</button>
    </form>
    <% if (data) { %>
        <h1>Account of @<%= data.name %>
        </h1>
        <h3>Followers: <%= data.followers %> Following: <%= data.following %>
        </h3>
        <button>Follow</button><button>message</button>
        <hr>
        <% for(let post of data.posts) { %>
            <img src="<%= post.image %>" alt="user Image">
            <p>
                Likes: <%= post.likes %> Comments: <%= post.comments %>
            </p>
            <% } %>
                <% } %>
</body>

</html>